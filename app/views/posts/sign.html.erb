<%
  instagram_sign_url = "https://api.instagram.com/oauth/authorize?" \
                        "client_id=#{ENV['INSTAGRAM_CLIENT_ID']}" \
                        "&redirect_uri=#{ENV['INSTAGRAM_REDIRECT_URI']}" \
                        "&scope=user_profile,user_media&response_type=code"
%>
<%= button_to "Sign in with Instagram", instagram_sign_url, data: { turbo: false } %>
